services:
  app:
    environment:
      NODE_ENV: development
    build:
      context: .
      dockerfile: Dockerfile 
    command: make dev
    ports:
      - "8080:8080"
    volumes:
      - "./app:/app"
